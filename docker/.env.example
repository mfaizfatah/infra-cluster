# Environment variables untuk Docker Compose
# Optimized untuk VPS 1GB RAM
#
# Cara generate password kuat:
#   openssl rand -base64 24

# === Database Configuration ===
POSTGRES_DB=myapp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=GANTI_DENGAN_PASSWORD_KUAT
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PASSWORD=GANTI_DENGAN_PASSWORD_KUAT
REDIS_PORT=6379

# === Monitoring Configuration (Optimized for 1GB RAM) ===
PROMETHEUS_PORT=9090
PROMETHEUS_RETENTION=7d

GRAFANA_PORT=3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=GANTI_DENGAN_PASSWORD_KUAT
GRAFANA_PLUGINS=

# === Proxy Configuration ===
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# === Profile-specific Variables (Memory Optimized) ===
# Database Profile
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=GANTI_DENGAN_PASSWORD_KUAT

# Monitoring Profile (Conservative settings)
MONITORING_USER=admin
MONITORING_PASSWORD=GANTI_DENGAN_PASSWORD_KUAT
MONITORING_RETENTION=7d

# Full Stack Profile (Not recommended for 1GB RAM)
FULL_DB_NAME=fullstack_db
FULL_DB_USER=fullstack_user
FULL_DB_PASSWORD=GANTI_DENGAN_PASSWORD_KUAT

# === Docker Configuration ===
COMPOSE_PROJECT_NAME=infra-cluster

# === Memory Optimization Notes ===
# For 1GB RAM VPS:
# - Use 'database' profile for development (PostgreSQL + Redis)
# - Use 'monitoring' profile sparingly (only when needed)
# - Avoid 'full' profile (will likely cause OOM)
# - Consider running services separately, not all at once
